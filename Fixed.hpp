#ifndef FIXED_HPP
#define  FIXED_HPP

class Fixed{

    private:
        int fixed;
        static const int shift;
    public:
        fixed_point(){};
        fixed_point_2();
        ~fixed_point();  
}

#endif
